<Window x:Class="MiddelbyReolsystemMVVM.Views.RackOverview"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="RackOverview" Height="450" Width="800"
        Background="Beige">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10">
            <Button Content="Reoloversigt" Margin="5" Padding="10" Background="LightGray" IsEnabled="False"/>
            <Button Content="Administrér reollejere" Margin="5" Padding="10" Background="LightPink"
                    Command="{Binding GoAdminRenter}"/>
            <Button Content="Administrér reoler" Margin="5" Padding="10" Background="LightPink"
                    Command="{Binding GoAdminRack}"/>
        </StackPanel>

        <Rectangle Grid.Row="1" Height="1" Fill="Black" Margin="10,5"/>

        <Grid Grid.Row="2">
            <Image Width="106" Height="80" Stretch="Uniform"
                   HorizontalAlignment="Left" VerticalAlignment="Top"
                   Source="/Images/MiddelbyLogo.png" Margin="-2,-82,0,0"/>
            <ListBox d:ItemsSource="{d:SampleData ItemCount=5}" Margin="38,51,390,64"/>
            <Button Content="Ledige" HorizontalAlignment="Left" Margin="84,26,0,0" VerticalAlignment="Top"/>
            <Button Content="Optagede" HorizontalAlignment="Left" Margin="149,26,0,0" VerticalAlignment="Top" Background="LightPink"/>
            <Button Content="Andet/reperation" HorizontalAlignment="Left" Margin="224,26,0,0" VerticalAlignment="Top" Background="LightPink"/>
        </Grid>
    </Grid>
</Window>
