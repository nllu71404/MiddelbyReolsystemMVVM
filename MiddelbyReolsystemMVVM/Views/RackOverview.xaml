<Window x:Class="MiddelbyReolsystemMVVM.Views.RackOverview"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:viewmodels="clr-namespace:MiddelbyReolsystemMVVM.Viewmodels" d:DataContext="{d:DesignInstance Type=viewmodels:RackViewModel}"
        mc:Ignorable="d"
        Title="RackOverview" Height="750" Width="1200"
        MinHeight="520" MinWidth="900"
        Background="Bisque" Loaded="Window_Loaded">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Width" Value="110"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Background" Value="#FFFFCCD4"/>
            <Setter Property="Foreground" Value="DarkRed"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            CornerRadius="25"
                            BorderBrush="DarkRed"
                            BorderThickness="2"
                            Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FFCC99A0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="432"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <!-- Venstre side: knapper og ListBox -->
        <StackPanel Grid.Column="0" Margin="40,20,20,40">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <Button Content="Ledige" Click="ShowAvailable_Button_Click" Margin="0,20,10,10"/>
                <Button Content="Optaget" Click="ShowOccupied_Button_Click" Margin="0,20,10,10"/>
                <Button Content="Andet" Click="ShowOther_Button_Click" Margin="0,20,10,10"/>
            </StackPanel>

            <ListBox ItemsSource="{Binding DisplayedRacks}" SelectedItem="{Binding SelectedRack}" Height="400" Width="250">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock FontSize="14" Margin="5">
                            <Run Text="Rack  "/>
                            <Run Text="{Binding RackNumber}"/>
                            <Run Text=" - " />
                            <Run Text="{Binding RackType.Name, Mode=OneWay}"/>
                        </TextBlock>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <ComboBox ItemsSource="{Binding Renters}" 
                      SelectedItem="{Binding SelectedRenter, Mode=TwoWay}" 
                      DisplayMemberPath="FirstName" 
                      Width="250" Margin="0,20,0,0" RenderTransformOrigin="0.564,3.172"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Tilføj lejer" Click="AssignRenter_Button_Click" Margin="0,20,10,0"/>
                <Button Content="Fjern lejer" Click="RemoveRenter_Button_Click" Margin="0,20,10,0"/>
            </StackPanel>
        </StackPanel>

        <!-- Højre side: resten af indholdet -->
        <DockPanel Grid.Column="1" Margin="0,20,0,0" HorizontalAlignment="Center" Width="630">
            <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Top">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,-50">
                    <Button Content="Reoloversigt" Background="#FFCC99A0" IsEnabled="False" Width="190" Height="50" Margin="10" FontWeight="Bold" FontSize="16"/>
                    <Button Content="Administrér reollejere" Click="OpenAdminRenterView_Button_Click" Width="190" Height="50" Margin="10" FontWeight="Bold" FontSize="16"/>
                    <Button Content="Administrér reoler" Click="OpenAdminRackView_Button_Click" Width="190" Height="50" Margin="10" FontWeight="Bold" FontSize="16"/>
                </StackPanel>
                <Image Height="590" Source="/Images/Oversigt.png" Width="382" RenderTransformOrigin="0.5,0.5">
                    <Image.RenderTransform>
                        <TransformGroup>
                            <ScaleTransform/>
                            <SkewTransform/>
                            <RotateTransform Angle="-90.00"/>
                            <TranslateTransform/>
                        </TransformGroup>
                    </Image.RenderTransform>
                </Image>
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>
