<Window x:Class="MiddelbyReolsystemMVVM.Views.AdminRackView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="AdminRackView" Height="600" Width="900"
        Background="Beige">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- knapper -->
            <RowDefinition Height="Auto"/>
            <!-- streg -->
            <RowDefinition Height="*"/>
            <!-- indhold -->
        </Grid.RowDefinitions>

        <!-- Knapper -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10">
            <Button Content="Reoloversigt"
                    Margin="5" Padding="10" Background="LightPink"
                    Command="{Binding GoRackOverview}"/>

            <Button Content="Administrér reollejere"
                    Margin="5" Padding="10" Background="LightPink"
                    Command="{Binding GoAdminRenter}"/>

            <Button Content="Administrér reoler"
                    Margin="5" Padding="10" Background="LightGray"
                    IsEnabled="False"/>
        </StackPanel>

        <!-- Streg -->
        <Rectangle Grid.Row="1" Height="1" Fill="Black" Margin="10,5"/>

        <!-- Indhold -->
        <Grid Grid.Row="2">
            <TextBlock Text="Her kommer Admin Reoler-indhold"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       FontSize="20"/>
        </Grid>
    </Grid>
</Window>