<Window x:Class="MiddelbyReolsystemMVVM.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        Background="Beige">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="13*"/>
            <ColumnDefinition Width="27*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Left"
                    Margin="214,10,0,10" Grid.ColumnSpan="2">
            <Button Content="Reoloversigt" Margin="5" Padding="10" Background="LightPink"
                    Command="{Binding OpenRackCommand}"/>
            <Button Content="Administrér reollejere" Margin="5" Padding="10" Background="LightPink"
                    Command="{Binding OpenRenterCommand}"/>
            <Button Content="Administrér reoler" Margin="5" Padding="10" Background="LightPink"
                    Command="{Binding OpenAdminRackCommand}"/>
        </StackPanel>

        <Rectangle Grid.Row="1" Height="1" Fill="Black" Margin="10,5,10,5" Grid.ColumnSpan="2"/>

        <Grid Grid.Row="2" Grid.ColumnSpan="2">
            <Image Width="215" Height="196" Stretch="Uniform"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   Source="/Images/MiddelbyLogo.png"/>
        </Grid>
    </Grid>
</Window>
